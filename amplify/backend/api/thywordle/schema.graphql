type GameStats @model @auth(rules: [{allow: public}, {allow: private}]) {
  id: ID!
  winDistribution: Int
  gamesFailed: Int
  currentStreak: Int
  bestStreak: Int
  totalGames: Int
  successRate: Int
  userID: ID
}
 